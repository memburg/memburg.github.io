---
export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	* {
		margin: 0;
		padding: 0;
		display: block;
	}

	:root {
		--colour-bg: rgb(224,217,217);
		--viewport-max-height: 100%;
		--page-width: 100%;
		--page-max-width: 1280px;
	}

	html,
	body {
		/* Height setup */
		height: var(--viewport-max-height);
		max-height: var(--viewport-max-height);

		/* Width setup */
		width: var(--page-width);
		max-width: var(--page-max-width);

		/* Background setup */
		background-color: var(--colour-bg);

		/* Center content */
		margin: 0 auto;

		font-size: 10px;
	}
</style>

<style is:global>
	@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Herr+Von+Muellerhoff&family=Roboto:wght@300&display=swap');

	@keyframes fadeIn {
        0% {
            opacity: 0;
        }
        100% {
            opacity: 1;
        }
    }
</style>